<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".forget_password"
    android:background="@drawable/signin">

    <EditText
        android:id="@+id/reset_pass_email"
        android:layout_width="300dp"
        android:layout_height="53dp"
        android:autofillHints="name"
        android:backgroundTint="#00000000"
        android:hint="Email"
        android:inputType="text"
        android:layout_marginBottom="200dp"
        android:textColorHint="@color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/imageView8"
        android:layout_width="20dp"
        android:layout_height="wrap_content"
        android:contentDescription="TODO"
        android:src="@drawable/ic_action_email"
        app:layout_constraintBottom_toBottomOf="@+id/reset_pass_email"
        app:layout_constraintEnd_toStartOf="@+id/reset_pass_email"
        app:layout_constraintTop_toTopOf="@+id/reset_pass_email" />

    <Button
        android:id="@+id/Reset_pass_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:backgroundTint="@color/water"
        android:text="valider"
        app:layout_constraintEnd_toEndOf="@+id/reset_pass_email"
        app:layout_constraintStart_toStartOf="@+id/reset_pass_email"
        app:layout_constraintTop_toBottomOf="@+id/reset_pass_email" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reset Password"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="25sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/reset_pass_email"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/reset_pass_Progress"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="MissingConstraints" />

</androidx.constraintlayout.widget.ConstraintLayout>